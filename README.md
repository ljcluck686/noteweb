# 📝 Vue Notes App

一个基于 Vue 3 和 Supabase 的现代化笔记应用，提供完整的笔记管理功能。

## ✨ 功能特性

- 🔐 **用户认证** - 完整的注册/登录系统
- 📝 **笔记管理** - 创建、编辑、删除笔记
-[object Object]**分类系统** - 自定义分类和颜色标记
- ⭐ **收藏功能** - 标记重要笔记
- 🔍 **实时搜索** - 快速查找笔记内容
- 🏷️ **标签系统** - 为笔记添加标签
- 💾 **自动保存** - 防止内容丢失
- 📊 **数据统计** - 查看笔记统计信息
-[object Object]导出** - 导出所有数据为 JSON
- 🎨 **现代化 UI** - 基于 Tailwind CSS 的美观界面

## 🛠️ 技术栈

- **前端框架**: Vue 3 (Composition API)
- **路由**: Vue Router 4
- **样式**: Tailwind CSS
- **后端服务**: Supabase
- **数据库**: PostgreSQL (Supabase)
- **认证**: Supabase Auth
- **构建工具**: Vue CLI

## 🚀 快速开始

### 前置要求

- Node.js (版本 14 或更高)
- npm 或 yarn
- Supabase 账户

### 安装步骤

1. **克隆项目**
   ```bash
   git clone <your-repo-url>
   cd vue-notes-app
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **配置 Supabase**
   - 在 Supabase 控制台创建新项目
   - 复制项目 URL 和 API Key
   - 更新 `src/lib/supabase.js` 中的配置信息

4. **设置数据库**
   - 在 Supabase SQL 编辑器中执行 `database-setup.sql` 文件中的 SQL 语句
   - 这将创建必要的表结构和安全策略

5. **启动开发服务器**
   ```bash
   npm run serve
   ```

6. **访问应用**
   打开浏览器访问 `http://localhost:8080`

## 📁 项目结构

```
src/
├── assets/          # 静态资源
│   └── styles.css   # 全局样式
├── components/      # 可复用组件
├── lib/            # 工具库
│   └── supabase.js # Supabase 配置和 API
├── router/         # 路由配置
│   └── index.js
├── views/          # 页面组件
│   ├── Home.vue    # 主页/笔记列表
│   ├── Login.vue   # 登录页面
│   ├── NoteEditor.vue # 笔记编辑器
│   └── Settings.vue   # 设置页面
├── App.vue         # 根组件
└── main.js         # 应用入口
```

## 🗄️ 数据库架构

### 主要表结构

- **user_profiles** - 用户配置信息
- **categories** - 笔记分类
- **notes** - 笔记内容

详细的数据库结构请查看 `database-setup.sql` 文件。

##[object Object]配置说明

### Supabase 配置

在 `src/lib/supabase.js` 中更新你的 Supabase 配置：

```javascript
const supabaseUrl = 'YOUR_SUPABASE_URL'
const supabaseKey = 'YOUR_SUPABASE_ANON_KEY'
```

## 📱 页面功能

### 🏠 主页 (Home)
- 显示所有笔记的网格布局
- 按分类和收藏状态筛选
- 实时搜索功能
- 创建和管理分类

### ✏️ 笔记编辑器 (NoteEditor)
- 富文本编辑界面
- 自动保存功能
- 标签和分类管理
- 笔记删除功能

### ⚙️ 设置页面 (Settings)
- 个人信息管理
- 分类管理
- 数据统计查看
- 数据导出功能

## 🔒 安全特性

- 行级安全策略 (RLS)
- 用户数据隔离
- 安全的认证流程
- 数据加密传输

## 🚀 部署

### Vercel 部署

1. 将项目推送到 GitHub
2. 在 Vercel 中导入项目
3. 设置环境变量
4. 部署完成

### Netlify 部署

1. 构建项目：`npm run build`
2. 将 `dist` 文件夹部署到 Netlify
3. 配置环境变量

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License
